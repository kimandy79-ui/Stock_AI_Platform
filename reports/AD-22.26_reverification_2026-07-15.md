# AD-22.26 Re-verification — consolidation_base Score Rescale

**Date:** 2026-07-15
**Scope:** Independent re-verification of the prior session's implementation
report (`reports/ad_22_26_option1_rescale_implementation_2026-07-15.md`).
No code changes made — verification only, per the requesting coder note.
This session started fresh with no memory of the prior implementation
session; found the implementation, tests, and report already present on
disk and uncommitted (`git status` shows the three files still unstaged
against `HEAD` commit `fbc5138`), and re-derived every claimed result
independently rather than trusting the prior report at face value.

---

## 1. Diff check

`git diff -- app/services/screening/m14_setup_validators.py` against `HEAD`
produces exactly two hunks: the `_CONSOLIDATION_BASE_RESCALE_A/B` constant
block (after `_CONFIDENCE_MEDIUM_THRESHOLD`) and the transform application
(immediately after `penalized_score = _clamp(raw_score + earnings_pen +
macro_pen + fundamentals_adj)` inside `validate_consolidation_base`). No
other line in the file differs from `HEAD`. **Matches the prior report's
§2 exactly.**

`git diff -- specs/02b_ARCHITECTURE_DECISIONS.md` against `HEAD` shows the
header fix (`(pending sign-off)` removed) plus the DRAFT→DECIDED status
rewrite and supporting sections — this was from an earlier coder-note pass
(the "prior coder-note pass" the implementation report references), separate
from and prior to the code implementation itself. Confirmed consistent with
that history.

## 2. Targeted regression tests (fresh run)

```
python -m pytest tests/test_m14_setup_validators.py -k TestConsolidationBaseRescaleAD2226 -v
```

Result: **11 passed, 173 deselected in 1.24s.** Matches the prior report's
§3 exactly (11/11).

## 3. Full module suite (fresh run)

```
python -m pytest tests/test_m14_setup_validators.py -v
```

Result: **184 passed in 10.74s.** Matches the prior report's §4.1 exactly
(184/184). Disk space checked before and after (`Get-PSDrive C`): ~2.68GB
free throughout, stable — the disk-exhaustion issue from the prior session
(§6.2 of the implementation report) did not recur for this scoped run.

## 4. Isolation re-proof (independent stash-based comparison)

Reproduced the prior report's §4.2 methodology from scratch rather than
trusting the pinned test alone:

1. Computed `breakout`/`pullback`/`trend_continuation` canonical-fixture
   scores against the **current working tree** (post-AD-22.26):
   `76.44444444444444` / `75.63470319634703` / `78.34649122807018`.
2. `git stash push --keep-index -- app/services/screening/m14_setup_validators.py`
   to isolate just that file back to `HEAD` (pre-AD-22.26).
3. Recomputed the same three fixture scores against the stashed (pre-change)
   source, using a standalone script with the fixtures inlined (the test
   module's own fixtures couldn't be imported against the pre-change source,
   since the test file — itself modified — imports the new constants at
   module scope): **identical values**, `76.44444444444444` /
   `75.63470319634703` / `78.34649122807018`.
4. `git stash pop` to restore the AD-22.26 change. `git status` confirmed
   the working tree returned to the same three-file-modified state as
   before the stash.

**Result: exact match, independently reproduced.** No drift in the other
three setups' scoring.

## 5. Empirical spot-check (2 of 5 campaign dates, recomputed from stored data)

Queried `step4_analysis` directly (`data/duckdb/prod.duckdb`, read-only) for
`setup_type='consolidation_base'` on 2026-06-11 and 2026-07-02, and applied
`new_score = clamp(0.52 * setup_score + 48.0)` to each stored (pre-rescale)
`setup_score`, reclassifying `score_below_threshold` failures as `passed`
where `new_score >= 55` (hard-fail reasons are gate-independent of the score
transform and were left untouched; already-passing rows remain passing since
the transform is monotonic and maps `[55,100] -> [76.6,100]`).

| Date | n | Old pass | Old % | New pass | New % |
|---|--:|--:|--:|--:|--:|
| 2026-06-11 | 630 | 16 | 2.54% | 164 | 26.03% |
| 2026-07-02 | 642 | 8 | 1.25% | 115 | 17.91% |

**Matches the prior report's §5.1 rows for these two dates exactly**, including
the failure-reason breakdown shift (`score_below_threshold` count → 0 on both
dates, `range_tightness_too_low`/`price_above_base_high`/`price_below_base_low`
counts unchanged).

**HTGC (2026-07-02) candidate**, recomputed from stored `step5_proposals`
using `_W_SETUP=0.40` (confirmed in `step5_proposal_engine.py:141`) and the
default `setup_confirmation` weight `0.10` (confirmed at
`step5_proposal_engine.py:682`):

| Field | Old | New | Delta |
|---|--:|--:|--:|
| setup_score | 59.29 | 78.83 | +19.54 |
| proposal_score_raw | 59.09 | 66.91 | +7.82 |
| risk_score | 30.33 | 28.37 | −1.95 |
| risk_label | low | low | — |
| disposition (old) | BUY | — | raw_rank=3, diversified_rank=2 |

**Matches the prior report's §5.3 HTGC row exactly**, to two decimal places.

(A second `HTGC`/2026-07-02 row exists in `step5_proposals` — same ticker and
date, different setup_config_id/candidate, `disposition=REJECTED`,
`setup_score` 66.70→82.68 — not part of the original report's cited example;
noted here for completeness, not a discrepancy.)

## 6. Outcome

**Everything matches.** No discrepancy found between the prior session's
report and an independent re-derivation of every claimed number: the code
diff, both test-suite runs, the isolation proof, and the empirical spot-check
(2/5 dates plus the HTGC candidate) all reproduce exactly. Per the requesting
coder note's decision rule, `specs/02b_ARCHITECTURE_DECISIONS.md` §22.26 has
been updated to `Status: IMPLEMENTED (2026-07-15)`, with a condensed
implementation summary added (root-cause mechanism, transform + anchor
reasoning, ordering proof, empirical outcome, downstream-consumer findings,
known gaps) pointing to both this report and the original implementation
report rather than duplicating either in full.

No code was modified in this re-verification pass — the existing
implementation was already correct.

## Deliverables checklist

1. ✅ Diff-match confirmation — exact match, both files.
2. ✅ Fresh test results — targeted class (11/11) and full module suite
   (184/184).
3. ✅ Fresh isolation stash-comparison — exact match, independently
   reproduced via `git stash`.
4. ✅ 2-date empirical spot-check — exact match (2026-06-11, 2026-07-02),
   plus HTGC candidate exact match.
5. ✅ ADR update — status flipped to IMPLEMENTED, condensed reasoning added.
6. ✅ This report, saved to `reports/AD-22.26_reverification_2026-07-15.md`.

No commit made. Diffs (ADR text) and this report are ready for review/commit
at the architect's discretion, per current policy.
