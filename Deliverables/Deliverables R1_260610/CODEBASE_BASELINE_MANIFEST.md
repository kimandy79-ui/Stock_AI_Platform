# Codebase Baseline Manifest

Input stable codebase: `stock_ai_platform_260604-02.zip`

Cleaned output:

- Root `README.md` replaced with current M21 Dashboard V2 orientation.
- Stale codebase `docs/` folder removed.
- `app/`, `tests/`, `tools/`, `requirements.txt`, and `pyproject.toml` are not intentionally changed.

The codebase is implementation baseline only. Project Files are the active source of truth.
